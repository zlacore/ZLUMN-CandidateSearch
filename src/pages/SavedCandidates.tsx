const SavedCandidates = () => {
  // Create function to:
  // - Take saved candidates' info from fetch data
  // - Put info into react components

  
  return (
    <>
      <h1>Potential Candidates</h1>
    </>
  );
};

export default SavedCandidates;
